// Write a function, which receives two names as string parameters and a delimiter. Print the names joined by the
// delimiter.

function concatNames(name, name1, delimiter) {
    console.log(name + delimiter + name1);
}

concatNames('John', 'Smith', '->')
concatNames('Jan', 'White', '<->')
concatNames('Linda', 'Terry', '=>')